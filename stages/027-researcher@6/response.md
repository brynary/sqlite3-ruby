{
  "findings": [
    {
      "title": "Authorizer callback result translation fails open to SQLITE_IGNORE, permitting denied writes",
      "category": "authorization",
      "ruleId": "auth-bypass.authorizer-callback-result",
      "identity": {
        "anchor": "authorizer-result-translation"
      },
      "file": "ext/sqlite3/database.c",
      "line": 489,
      "symbol": "rb_sqlite3_auth",
      "snippet": "  return SQLITE_IGNORE;",
      "severity": "HIGH",
      "difficulty": "MEDIUM",
      "confidence": "MEDIUM",
      "impact": "An application that installs a SQLite authorizer to sandbox untrusted SQL (the documented purpose of sqlite3_set_authorizer) and whose deny branch returns nil -- exactly what the extension's own documentation at ext/sqlite3/database.c:500 and lib/sqlite3/database.rb:72 instructs -- does not deny writes. rb_sqlite3_auth translates nil to SQLITE_IGNORE, and per the SQLite contract SQLITE_IGNORE on SQLITE_INSERT/SQLITE_UPDATE/SQLITE_DELETE lets the statement compile and the mutation proceed rather than aborting it. The security control the application believes it installed is fail-open for every data-modifying action, allowing an attacker who supplies SQL text to insert, update, delete, drop or attach against a database the authorizer was meant to protect. The same translation makes any non-Fixnum truthy return (for example a Bignum, a String, or an object) silently permissive too.",
      "evidence": [
        "lib/sqlite3/database.rb:73 - Database#authorizer accepts an arbitrary application-supplied block and forwards it to the C setter via self.authorizer = block, so the block body is the entire policy.",
        "lib/sqlite3/database.rb:71 - the doc comment immediately above tells the developer 'If the block returns 0 (or nil), the statement is allowed to proceed. Returning 1 causes an authorization error ... and returning 2 causes the access to be silently denied', while ext/sqlite3/database.c:500 tells them the opposite for nil ('returning 2 or nil causes the access to be silently denied'); the two documented contracts for nil contradict each other, so a developer following either one writes a policy whose return values the C code does not honour as a deny.",
        "ext/sqlite3/database.c:508 - set_authorizer installs rb_sqlite3_auth as the connection's authorizer with self as user data, making the C function the only gate between prepared SQL and SQLite's action checks.",
        "ext/sqlite3/database.c:514 - the caller's callable is stored in @authorizer, which rb_sqlite3_auth reads back at ext/sqlite3/database.c:482 on every authorization decision.",
        "ext/sqlite3/statement.c:53 - attacker-influenced SQL text reaches sqlite3_prepare_v2; per /usr/include/sqlite3.h:3151-3155 this is precisely when SQLite invokes the registered authorizer for each action code, so every prepare of untrusted SQL runs the gate at ext/sqlite3/database.c:468.",
        "ext/sqlite3/database.c:483 - the Ruby policy is invoked and its return value captured in result; this single VALUE is the whole access decision.",
        "ext/sqlite3/database.c:485 - guard checked and found insufficient: only T_FIXNUM returns are passed through as a raw code, so an integer-like deny value that is not a Fixnum (a Bignum) skips this branch entirely.",
        "ext/sqlite3/database.c:487 - guard checked and found insufficient: only the exact object Qfalse maps to SQLITE_DENY; nil, non-Fixnum numerics, strings and arbitrary objects all miss both this line and line 486.",
        "ext/sqlite3/database.c:489 - the sink: every unmatched return value, nil included, becomes SQLITE_IGNORE. There is no default-deny branch and no validation that the policy returned a recognised value.",
        "/usr/include/sqlite3.h:3253 - #define SQLITE_IGNORE 2 is documented as 'Don't allow access, but don't generate an error', which is what makes the misreading plausible, but the action-specific semantics below override this summary.",
        "/usr/include/sqlite3.h:3194 - the authoritative contract: 'If the action code is [SQLITE_DELETE] and the callback returns [SQLITE_IGNORE] then the [DELETE] operation proceeds' -- so the value returned at ext/sqlite3/database.c:489 permits the mutation instead of blocking it, and the intended deny never takes effect.",
        "test/test_database.rb:231 - the regression test that masks the bug: test_authorizer_ignore returns nil from the policy but only exercises SELECT 'fooooo', where SQLITE_IGNORE substitutes a NULL column value, so stmt.step returning nil at test/test_database.rb:236 looks like a successful deny. No test covers a nil-returning policy against INSERT, UPDATE or DELETE, which is where the fail-open behaviour appears.",
        "Not verified by execution: I did not build or run this extension, so the INSERT/DELETE-proceeds behaviour rests on the sqlite3.h contract at /usr/include/sqlite3.h:3194-3195 rather than on observed output. Confidence is MEDIUM for that reason, and because exploitation depends on the victim application's policy returning nil or another unmatched value on its deny path."
      ],
      "rationale": "This is a concrete authorization bypass, not a style note: the C function at ext/sqlite3/database.c:468 is the sole translation point between an application's access policy and SQLite's compile-time authorization gate, and its default branch at line 489 converts every unrecognised policy return value into SQLITE_IGNORE. SQLITE_IGNORE is not a deny for data-modifying actions -- /usr/include/sqlite3.h:3194 states plainly that a DELETE proceeds when the callback returns it. The path from untrusted source to dangerous operation is complete and short: attacker-influenced SQL enters sqlite3_prepare_v2 at ext/sqlite3/statement.c:53, SQLite calls rb_sqlite3_auth, the application's deny value (nil) misses the T_FIXNUM check at line 485 and the Qfalse check at line 487, and the write is permitted. What elevates this from a latent API sharp edge to a live vulnerability is that the extension's own documentation actively directs developers into the vulnerable pattern -- ext/sqlite3/database.c:500 says nil denies access -- and the repository's only test for that value, test/test_database.rb:231, uses a SELECT, where SQLITE_IGNORE coincidentally NULLs the column and mimics a successful deny. So the defect is documented as safe and covered by a test that passes for the wrong reason. Severity is HIGH because the bypassed control is the mechanism SQLite documents for sandboxing untrusted SQL, and its failure grants arbitrary writes, schema changes and ATTACH. Difficulty is MEDIUM because the attacker needs no special tooling once the precondition holds, but the precondition -- a victim policy returning nil or a Bignum on its deny path -- is not universal. Confidence is MEDIUM rather than HIGH because I did not execute the extension; the behaviour is established from the SQLite header contract and the code's control flow, both read directly.",
      "exploitScenarios": [
        "A developer sandboxes untrusted SQL by calling db.authorizer { |action, a, b, c, d| action == SQLite3::Constants::ErrorCode::OK ? 0 : nil }, or any policy whose deny branch is a bare nil / implicit nil from a trailing if without an else, following the documentation at lib/sqlite3/database.rb:72 that states nil means the access is denied.",
        "The application passes attacker-supplied SQL to Database#execute or #prepare, reaching sqlite3_prepare_v2 at ext/sqlite3/statement.c:53.",
        "SQLite invokes rb_sqlite3_auth at ext/sqlite3/database.c:468 with action code SQLITE_DELETE (9) or SQLITE_INSERT for the attacker's statement.",
        "The Ruby policy returns nil, intending to deny.",
        "The type checks at ext/sqlite3/database.c:485-487 all miss nil, so control reaches ext/sqlite3/database.c:489 and the function returns SQLITE_IGNORE.",
        "Per /usr/include/sqlite3.h:3194, SQLite compiles the statement and the DELETE (or INSERT/UPDATE) proceeds; the attacker's write lands in the database and no SQLite3::AuthorizationException is raised, so the application logs no policy violation.",
        "The attacker repeats with DROP TABLE, ATTACH or UPDATE statements to escalate from data modification to schema and cross-file access."
      ],
      "preconditions": [
        "The application installs an authorizer via Database#authorizer or #authorizer= (ext/sqlite3/database.c:502); the authorizer is disabled by default, so this is a non-default configuration.",
        "The application relies on the authorizer as a security boundary for SQL text it does not fully control.",
        "The policy's deny path returns a value other than false or a Fixnum -- most importantly nil, which both the C comment at ext/sqlite3/database.c:500 and the Ruby comment at lib/sqlite3/database.rb:72 present as a deny -- or returns a non-Fixnum integer such as a Bignum.",
        "The attacker can influence the SQL text that is prepared, or the action codes reached during preparation."
      ],
      "recommendations": [
        "Root-cause fix: make the translation fail closed. Return SQLITE_DENY for any value the policy did not explicitly authorize -- replace the fallthrough at ext/sqlite3/database.c:489 with SQLITE_DENY, and validate the T_FIXNUM branch at line 485 so only SQLITE_OK, SQLITE_DENY and SQLITE_IGNORE are accepted, raising or denying on anything else. Handle nil explicitly as SQLITE_DENY rather than letting it reach the default.",
        "Hardening: reconcile the two contradictory doc comments at ext/sqlite3/database.c:498-500 and lib/sqlite3/database.rb:70-72 with the implemented behaviour, and expose SQLITE_OK/SQLITE_DENY/SQLITE_IGNORE as named SQLite3 constants so policies return an unambiguous symbol instead of an ad-hoc integer or nil.",
        "Regression test: extend test/test_database.rb beyond the SELECT-only test_authorizer_ignore at line 231 with cases that install a policy returning nil, a Bignum, a String and an arbitrary object, then assert that an INSERT, an UPDATE and a DELETE each raise SQLite3::AuthorizationException and that the target table's contents are unchanged afterward."
      ]
    },
    {
      "title": "User-defined function Proc passed to SQLite as unrooted void* user data, enabling use-after-free on callback",
      "category": "memory",
      "ruleId": "race-condition.callback-user-data-lifetime",
      "identity": {
        "anchor": "udf-callback-user-data-retention"
      },
      "file": "ext/sqlite3/database.c",
      "line": 326,
      "symbol": "define_function",
      "snippet": "    (void *)block,",
      "severity": "HIGH",
      "difficulty": "HIGH",
      "confidence": "MEDIUM",
      "impact": "The Ruby Proc that implements a user-defined SQL function is handed to SQLite as an opaque void* and is not reachable from any GC root afterward. Once the registering method returns, the Proc's only referent is a raw address inside SQLite's function table, invisible to Ruby's mark phase. If a garbage collection runs before or between invocations of that SQL function, the object can be swept and its slot reused; the next invocation of the function reads the stale address at ext/sqlite3/database.c:288 and calls rb_funcall2 on it at line 295. That is a use-after-free on a Ruby object where the callee is determined by whatever now occupies the recycled slot, giving an attacker who can influence heap layout and trigger the SQL function a path to calling an arbitrary method on an arbitrary object -- i.e. control-flow hijack inside the interpreter, or at minimum a crash. define_aggregator has the same defect with a partial mitigation: ext/sqlite3/database.c:390 pins only the most recently registered aggregator in a single @agregator ivar, so registering a second aggregate under a different name drops the first object's only reference while SQLite still holds its address for the first function name.",
      "evidence": [
        "ext/sqlite3/database.c:319 - define_function materialises the caller's block as a Proc VALUE in a C stack local; this is the object that must stay alive for as long as the SQL function is registered.",
        "ext/sqlite3/database.c:326 - the sink: the VALUE is cast to void* and stored as SQLite's pApp user data. A VALUE hidden behind void* inside a third-party library's table is not scanned by Ruby's garbage collector.",
        "ext/sqlite3/database.c:334 - define_function returns without storing the Proc anywhere Ruby can see; there is no rb_iv_set in this function body, unlike trace (ext/sqlite3/database.c:169), busy_handler (:213) and set_authorizer (:514), which all pin their callable in an ivar and pass (void *)self instead. The Proc becomes unreachable at this point.",
        "ext/sqlite3/database.c:27 - guard checked and found absent: the Database object is wrapped with Data_Wrap_Struct(klass, NULL, deallocate, ctx), i.e. a NULL mark function, so even a VALUE stored in the C struct would not be marked -- and struct _sqlite3Ruby at ext/sqlite3/database.h:6-8 has no VALUE slot at all.",
        "ext/sqlite3/database.c:1 - guard checked and found absent: a repository-wide search of ext/ finds no rb_gc_mark, rb_gc_register_address, rb_gc_register_mark_object, rb_global_variable, RB_GC_GUARD, TypedData_Wrap_Struct or rb_data_type_t anywhere, so no other mechanism roots the Proc.",
        "lib/sqlite3/database.rb:259 - crossing into the Ruby layer: create_function passes a freshly created wrapper block to define_function and stores neither the wrapper nor the caller's &block in an ivar, constant, global or collection, so the entire closure chain becomes garbage as soon as create_function returns at lib/sqlite3/database.rb:265.",
        "ext/sqlite3/statement.c:127 - the reachable trigger: sqlite3_step is the only place this extension executes a prepared statement, and it is exposed to Ruby as Statement#step (ext/sqlite3/statement.c:385). Any SQL naming the registered function -- via Database#execute (lib/sqlite3/database.rb:115), #get_first_value (:229), Statement#each (lib/sqlite3/statement.rb:106) and so on -- causes SQLite to call back into rb_sqlite3_func.",
        "ext/sqlite3/database.c:288 - the callback recovers the Proc with VALUE callable = (VALUE)sqlite3_user_data(ctx), reading back the raw address with no liveness check and no type check.",
        "ext/sqlite3/database.c:295 - the dangerous operation: rb_funcall2(callable, rb_intern(\"call\"), argc, params) invokes a method on that address. If the object was collected and its slot reused, this dispatches on an unrelated object of whatever class now occupies the slot.",
        "ext/sqlite3/database.c:390 - the aggregate variant: rb_iv_set(self, \"@agregator\", aggregator) is a single ivar slot on the Database, so the Nth registration evicts the (N-1)th aggregator object while SQLite retains its address as pApp for the earlier function name (registered at ext/sqlite3/database.c:384). @agregator is written here and read nowhere in the repository, confirming it exists only as a retention anchor and that it cannot retain more than one object.",
        "ext/sqlite3/database.c:353 - the aggregate callback path reaches rb_funcall2(callable, rb_intern(\"step\"), ...) on that potentially collected address, with rb_sqlite3_final doing the same at ext/sqlite3/database.c:360.",
        "Not verified by execution: I did not build the extension or run a GC-stress reproduction, so I have not observed a collected Proc being invoked. The claim rests on the absence of any GC root in ext/ (verified by search) plus Ruby's mark-and-sweep reachability rules. Whether a given interpreter build promptly reuses the freed slot, and whether an attacker can steer what lands there, determines how far past a crash the impact reaches -- hence MEDIUM confidence and HIGH difficulty."
      ],
      "rationale": "This is a genuine memory-safety defect with a complete path, not a mere unsafe-looking API. The root control is the single line that launders a Ruby VALUE through void* at ext/sqlite3/database.c:326, and I confirmed by exhaustive search rather than assumption that nothing else keeps the object alive: there is no mark function (the Data_Wrap_Struct at line 27 passes NULL), the wrapped struct at ext/sqlite3/database.h:6-8 has no VALUE slot that a mark function could even reach, no rb_gc_* or rb_global_variable call exists anywhere in ext/, and neither define_function nor its Ruby caller create_function (lib/sqlite3/database.rb:258-265) stores the callable in any ivar, constant, global or collection. The contrast with the sibling callbacks is what proves this is an oversight rather than an intended pattern: trace, busy_handler and authorizer= all pass (void *)self and re-fetch their callable from an ivar inside the callback, which is exactly the safe shape define_function omits. The dangerous operation is reached through ordinary use -- rb_funcall2 on the recovered address at line 295, triggered by any query naming the function, since Statement#step at ext/sqlite3/statement.c:127 is the one execution path in the extension. The aggregate case at line 390 is the same root control with a weaker mitigation worth noting under this finding rather than as a separate one: a single-slot ivar cannot retain more than one aggregator, so a second registration under a different name deterministically unroots the first while SQLite still holds its pointer, removing the need for GC-slot grooming. Severity is HIGH because a use-after-free that lands in rb_funcall2 means method dispatch on an attacker-influenceable object inside the interpreter. Difficulty is HIGH because turning that into controlled dispatch requires interpreter-internals knowledge, allocation grooming and favourable GC timing. Confidence is MEDIUM because I did not build or GC-stress the extension; the unreachability is proven from the code, but the exploitability beyond a crash is inferred.",
      "exploitScenarios": [
        "An application registers a SQL helper with db.create_function('helper', 1) { |func, v| ... } (lib/sqlite3/database.rb:258) or calls define_function directly; the Proc created at ext/sqlite3/database.c:319 is stored only as SQLite pApp at line 326.",
        "create_function returns, leaving the Proc unreachable from every GC root, as confirmed by the absence of any mark function or retaining ivar.",
        "The attacker drives ordinary application traffic that allocates and discards Ruby objects until a major GC runs, sweeping the Proc and freeing its heap slot.",
        "The attacker continues issuing allocations chosen so that a controlled object -- one whose class defines a #call with useful side effects, or one whose internals the attacker has shaped -- is placed in the recycled slot.",
        "The attacker then causes any query naming the registered function to be prepared and stepped, reaching sqlite3_step at ext/sqlite3/statement.c:127 and thus rb_sqlite3_func.",
        "ext/sqlite3/database.c:288 reads the stale address and ext/sqlite3/database.c:295 invokes #call on the object now in that slot, with attacker-influenced SQL argument values passed as parameters, yielding an unintended method invocation inside the interpreter or a segmentation fault.",
        "For the aggregate variant the attacker needs no GC-slot grooming beyond the eviction itself: the application registers two differently named aggregates via create_aggregate or create_aggregate_handler (lib/sqlite3/database.rb:338, :403), the second rb_iv_set at ext/sqlite3/database.c:390 drops the first aggregator, and a query using the first aggregate name reaches rb_funcall2 at ext/sqlite3/database.c:353 on the freed object."
      ],
      "preconditions": [
        "The application registers at least one user-defined SQL function or aggregate through Database#define_function, #create_function, #define_aggregator, #create_aggregate or #create_aggregate_handler; applications that use none of these are unaffected.",
        "A garbage collection sweeps the unrooted Proc or aggregator object between registration and a later invocation of the SQL function.",
        "The attacker can cause SQL naming that function to be prepared and stepped, which for the UDF case is satisfied by any normal application query that uses the helper.",
        "To move beyond a crash to controlled method dispatch, the attacker needs enough influence over Ruby allocation to place a chosen object in the freed slot -- detailed interpreter knowledge and favourable timing, which is why difficulty is HIGH.",
        "For the aggregate eviction path, the application must register two or more aggregates with different names on the same Database connection and then use the earlier one."
      ],
      "recommendations": [
        "Root-cause fix: stop passing bare Ruby VALUEs as SQLite user data without a GC root. Give the Database wrapper a mark function instead of the NULL at ext/sqlite3/database.c:27 (moving to TypedData_Wrap_Struct with a dmark that marks a retained collection), or -- matching the pattern trace/busy_handler/authorizer= already use -- pass (void *)self as pApp, keep the callables in a per-Database Hash keyed by function name and arity, and look the callable up by name inside rb_sqlite3_func rather than dereferencing a raw address.",
        "Hardening: replace the single @agregator slot at ext/sqlite3/database.c:390 with a per-name collection so registering additional aggregates cannot evict a still-registered one, and fix the ivar's misspelling while doing so. Consider refusing to re-register a name whose previous callable is still referenced by SQLite, and validate that the recovered user data is a Proc or responds to the expected method before rb_funcall2 at ext/sqlite3/database.c:295 and :353.",
        "Regression test: add a test that registers a UDF, drops every Ruby-side reference, forces collection with GC.stress or repeated GC.start plus heavy allocation, then executes a query calling that function and asserts the correct result rather than a crash; add a companion test that registers two differently named aggregates, then exercises the first one after the second registration and asserts its result, which fails today because of the @agregator eviction."
      ]
    }
  ]
}