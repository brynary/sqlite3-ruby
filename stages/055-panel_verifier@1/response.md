{"job_id": "panel:F3:defenses", "candidate_id": "F3", "finding_id": "csf_1bb6a6b12c7122aaf4f62e45", "occurrence_id": "occ_2a327b4a069a12fdf2313064", "lens": "DEFENSES", "verdict": "TRUE_POSITIVE", "vote": "TRUE_POSITIVE", "confidence": 0.8, "reasoning": "Verified the claim as written and then searched specifically for any mitigation that would keep the Proc alive; none exists. The sink is exact: ext/sqlite3/database.c:326 is `(void *)block,` inside `define_function` (ext/sqlite3/database.c:313-335), and the only handle on that object is the C local created at ext/sqlite3/database.c:319 (`VALUE block = rb_block_proc();`). Candidate defenses checked and each ruled out: (1) no GC mark function -- the Database wrapper is allocated with `Data_Wrap_Struct(klass, NULL, deallocate, ctx)` at ext/sqlite3/database.c:27, and the wrapped struct holds only a raw `sqlite3 *db` (ext/sqlite3/database.h:6-8), so there is no VALUE slot to mark even if a mark function existed; (2) no instance-variable root -- `grep -rn 'rb_iv_set|rb_ivar_set' ext/` returns only ext/sqlite3/database.c:82-87, 169 (@tracefunc), 213 (@busy_handler), 390 (@agregator), 514 (@authorizer), 547 (@encoding) and ext/sqlite3/statement.c:63-66; nothing stores a `define_function` block, and there is no `@functions` collection anywhere in ext/ or lib/; (3) no global registration -- `grep -rn 'rb_gc|rb_global_variable|rb_gc_register_address|RB_GC_GUARD' ext/` returns no matches (exit status 1); (4) no Ruby-side retention -- the documented wrapper `create_function` at lib/sqlite3/database.rb:258-265 builds a fresh inline block, forwards it to `define_function`, and returns `self` without storing the Proc, so the wrapper adds no root either; (5) no teardown that neutralizes the stale pointer -- `deallocate` (ext/sqlite3/database.c:9-22) only finalizes statements and closes the db, never re-registering the function with a NULL implementation. With no root, the Proc is unreachable from every GC root once `define_function` returns (the C local is in a dead frame below the stack pointer, so MRI's conservative machine-stack scan is timing luck, not a defense), while ext/sqlite3/database.c:288 recovers the raw VALUE via `sqlite3_user_data` and ext/sqlite3/database.c:295 performs `rb_funcall2(callable, rb_intern(\"call\"), argc, params)` on it -- a method dispatch that reads the klass pointer of possibly reclaimed (and possibly reused, hence type-confused) heap. Reachability is the library's primary public API: any application registering a custom SQL function and later executing SQL that calls it (exercised at test/test_database.rb:141-147 and test/test_integration.rb:469-476) walks this path, and an attacker who can drive queries and ordinary object allocation influences both the collection timing and the slot reuse. The aggregator path at ext/sqlite3/database.c:379-392 shows the author knew a root was needed (`rb_iv_set(self, \"@agregator\", aggregator)`), which underscores that its absence in `define_function` is an omission rather than an unstated invariant. I did not compile or run anything, so I did not observe an actual crash; the defense question, which is my lens, is fully settled by source inspection."}